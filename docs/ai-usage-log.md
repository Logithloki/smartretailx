# AI Usage Log — SmartRetailX

One line per session describing AI assistance (guide §0 rule 5 — academic-integrity declaration).
Harvard-referenced acknowledgement of AI tooling goes in the report's declaration section.

| Date | Tool | Assistance provided |
|------|------|---------------------|
| 2026-07 (design phase) | Claude (chat) | Architecture design review producing the ADR set (ADR-01..08), fix backlog 1–32, and the master implementation guide; adversarial self-review passes on the design and diagram |
| 2026-08-02 | Claude Code | Full read-only repository audit against CLAUDE.md/guide (gap table, contradictions, live-billing discovery); then, with approval, Week-0 hygiene fixes and Week-1 Terraform corrections (live toggle, internal ALB, data-subnet isolation, DynamoDB/SQS/ECR fixes, Aurora PG16 min-0, per-service IAM) |
| 2026-08-02 | Claude Code | W2 D1: shared srx_common auth module - fail-closed ENV handling, Cognito JWKS validation, cognito:groups RBAC dependency; 29 tests |
| 2026-08-02 | Claude Code | W2 D2: User Service - /v1/users/me, admin directory listing, self-vs-admin read rules, in-memory vs Cognito repositories; 14 tests |
| 2026-08-02 | Claude Code | W2 D3: Order Service - Pydantic validation, server-side totals, Decimal money, DynamoDB write, SQS command publish, userId-index reads; 27 tests |
| 2026-08-02 | Claude Code | W2 D4: idempotency - per-user scoped keys, payload fingerprinting, conditional-write claim, release on failure; 16 further tests |
| 2026-08-02 | Claude Code | W2 D5: saga compensation receiver, order-events queue + SNS filter policy in messaging.tf and localstack-init; found and fixed missing GSIs in the local seed script |
| 2026-08-02 | Claude Code | W3 D1: inventory stock model, atomic conditional-UPDATE reservation, CHECK constraint; 16 tests |
| 2026-08-02 | Claude Code | W3 D2: tenacity retry inside pybreaker circuit breaker; business refusals excluded from both; 9 tests |
| 2026-08-02 | Claude Code | W3 D3: saga outcome publisher (order-confirmed/order-rejected), SQS consumer, admin stock endpoints; 21 tests |
| 2026-08-02 | Claude Code | W3 D4: Product Service - TTL cache with X-Cache header, category GSI reads, admin CRUD; 29 tests |
| 2026-08-02 | Claude Code | W3 D5: full local saga demo against LocalStack + Postgres, both directions verified; scripts/saga-demo.sh |
| 2026-08-03 | Claude Code | W4 D1: notification Lambda with Powertools Logger/Tracer/Idempotency, SES identity in Terraform, event-carried userEmail through the saga; 14 tests |
| 2026-08-03 | Claude Code | W4 D2: Lambda Terraform (ARM64 + Powertools layer), SNS filter incl. loadTest exclusion, stock-reconciliation Lambda + enabled Asia/Colombo schedule, least-privilege Lambda IAM; 12 tests |
| 2026-08-03 | Claude Code | W4 D3: Powertools formatter adopted across all four Fargate services so services and Lambdas emit one log shape; correlation id injected via logging filter |
| 2026-08-03 | Cursor Agent | W5 chunk 1 commit 1: EventBridge Pipes — DDB Streams → orders event bus, native filter on MODIFY + terminal status, dedicated pipes IAM role; pipes.tf + outputs |
| 2026-08-03 | Cursor Agent | W5 chunk 1 commit 2: WebSocket API v2 + REQUEST Lambda authorizer validating Cognito JWT off ?token=; authorizer Lambda (8 tests) with offline JWKS stub |
| 2026-08-03 | Cursor Agent | W5 chunk 1 commit 3: connect / disconnect / push Lambdas — connect writes (connectionId, userId, ttl); disconnect idempotent delete; push scans by userId, prunes 410 GoneException inline, re-raises 5xx for EventBridge retries (15 tests) |
| 2026-08-03 | Cursor Agent | W5 chunk 1 commit 4: backlog 33 IAM — PutItem / DeleteItem / Scan-Query-Delete on websocket-connections split per Lambda; execute-api:ManageConnections scoped to this WSS API's stage ARN |
| 2026-08-03 | Cursor Agent | W5 chunk 1 commit 5: EventBridge rule + target + invoke permission — routes order.status-changed events off the bus into the push Lambda; terraform validate green |
| 2026-08-03 | Cursor Agent | Lambda cross-platform packaging fix: `pyjwt[crypto]` needs a Linux ARM64 wheel bundled into the authorizer zip (dev host is Windows) — added `scripts/build-lambda-packages.ps1` that runs `pip install --platform manylinux2014_aarch64 --only-binary=:all: --target infra/build/ws-authorizer-src`, switched the archive_file to `source_dir` |
| 2026-08-03 | Cursor Agent | Terraform correctness pass discovered by real API: (a) WS authorizer rejects `authorizer_result_ttl_in_seconds` and `authorizer_payload_format_version` (removed with comment); (b) WS stage `access_log_settings` requires account-level API GW CloudWatch role (added `aws_api_gateway_account` + `AmazonAPIGatewayPushToCloudWatchLogs` role) — one apply retry each |
| 2026-08-03 | Cursor Agent | **CW-4 + CW-5 live evidence window on real AWS.** Applied `live=true`, seeded Cognito test users (`logithsivakumar07@gmail.com` customer, `admin@example.com` admin) with `admin-set-user-password` from a shell env var, placed real orders, captured raw evidence: SES delivered email (MessageId `0102019fc88697ec-…`), idempotency wrapper returned cached MessageId on replay, reconciliation Lambda returned `stuck:0`, WebSocket authorizer allowed a valid Cognito ID token, live push arrived at the client (`{"type":"order.status-changed","orderId":"ord-f6c9745b753a","status":"CONFIRMED"}`), invalid token → HTTP 403 at handshake. Runbooks `docs/cw-4-runbook.md` and `docs/cw-5-runbook.md` + raw log `docs/cw-4-5-session-evidence.md` |
| 2026-08-03 | Cursor Agent | W5 chunk 2 commit 1: CloudFront + WAF + S3 (backlog 27 edge). SPA bucket with BPA + OAC-signed CloudFront reads; dual-origin distribution (S3 / and HTTP API /api/*); Managed-AllViewerExceptHostHeader + Managed-CachingDisabled on /api/* so the Authorization header survives (Addendum G.1.1); WAFv2 WebACL in us-east-1 via a new `aws.us_east_1` provider alias, CommonRuleSet + KnownBadInputs + SQLi count-only; Cognito app client callbacks + logout URLs now include the CloudFront domain; SPA-routing fallback of 403/404 → /index.html; outputs `spa_bucket_name`, `cloudfront_domain`, `cloudfront_distribution_id`, `waf_web_acl_arn` |
| 2026-08-03 | Cursor Agent | W5 chunk 2 commit 2: GitHub OIDC (backlog 10). `aws_iam_openid_connect_provider` for token.actions.githubusercontent.com (empty thumbprint - AWS validates via CA trust since 2023-07); `smartretailx-gha-deploy` role trust policy pins `aud=sts.amazonaws.com` (StringEquals) and `sub=repo:Logithloki/smartretailx:*` (StringLike - `:*` is a wildcard, StringEquals would never match); inline policy scopes ECR push to the service repos, ECS UpdateService + PassRole to the exact task/execution roles with `iam:PassedToService=ecs-tasks.amazonaws.com`, SPA bucket read/write, and CreateInvalidation on the single distribution ARN. `max_session_duration=3600`. Workflow `deploy.yml` gets `id-token: write`, matrix pytest+coverage, tflint + checkov, buildx-with-QEMU ARM64 push per service, Trivy fail-on-CRITICAL, ECS `update-service --force-new-deployment` (deployment_circuit_breaker auto-rollback), and SPA build + `aws s3 sync` + CloudFront invalidation |
| 2026-08-03 | Cursor Agent | W5 chunk 2 commit 3: Vite + React + TS SPA scaffold (backlog 27). react-oidc-context + oidc-client-ts (~15 KB, spec OIDC — no Amplify), authorization-code + PKCE, `automaticSilentRenew`, WebStorage user store, `onSigninCallback` scrubs `?code=` from the URL. `/products` protected route fetches `GET /v1/products` with `Authorization: Bearer <access_token>` — the W5 D4 "auth end-to-end proven" milestone. Build-time-only VITE_* env with a `required()` guard that fails fast on missing config; TypeScript widens `ImportMetaEnv` so a typo is a compile error. Minimal styling on purpose (full CRUD UX is W6 D1-3, backlog 28-30) |
| 2026-08-04 | Cursor Agent | W6 commit A: SPA foundations. Shared `apiFetch<T>` wrapper (Authorization + optional `Idempotency-Key` via `crypto.randomUUID`, typed `ApiError` on non-2xx). Hand-written TypeScript mirrors of Pydantic models — money as `string` on the wire so no `Number()` cast can reintroduce float drift the backend Decimal design eliminates. `useIsAdmin()` hook reads `cognito:groups` from the ID token; documented as UI convenience only (backend enforces `requires("admin")` independently). New routes + AdminRoute guard; ProductsPage refactored to use the shared client |
| 2026-08-04 | Cursor Agent | W6 commit B: Place Order page (marking Task 3). Product dropdown + quantity input; Decimal-safe cents preview total; `POST /v1/orders` with the shared `apiFetch`, opts.idempotent=true so a double-click cannot double-charge. 201 redirects to `/orders?highlight=<orderId>` so the My Orders page can flash the row on arrival. Documented `unitPrice` trust-boundary caveat in code — the backend currently accepts client-supplied prices (tampering vector); fix belongs in Order Service `create_order` (out of scope for W6) |
| 2026-08-04 | Cursor Agent | W6 commit C: My Orders + live WebSocket status (marking Task 4). `useOrderStatusStream(token, onUpdate)` opens a single WebSocket with `?token=<access_token>` (WS handshakes cannot set Authorization; Lambda authoriser reads the query string), receives `order.status-changed`, and merges into state. Exponential-backoff reconnect capped at 30 s; ref-based cleanup on unmount. Row-flash animation makes the PENDING→CONFIRMED flip visible during viva. Connection-phase badge in the header. New `VITE_WEBSOCKET_URL` env var wired through `required()` + TypeScript env types |
| 2026-08-04 | Cursor Agent | W6 commit D: Admin Products CRUD (backlog 28). Single-form-toggling create/edit component (avoids the "form unmount = lost focus" bug that separate render trees usually cause). Native form validation `pattern="[A-Za-z0-9._-]+"` and price `^\d+(\.\d{1,2})?$` mirror the Pydantic constraints so the server never rejects a shape the SPA declared valid. `PUT` sends a partial `ProductUpdate` body (all fields optional server-side). `DELETE` gated by `window.confirm` |
| 2026-08-04 | Cursor Agent | W6 commit E: Admin Stock page (backlog 30). GET `/v1/inventory` renders every row; per-row absolute-set input + Save button `PATCH`es `{quantity: N}`. Drafts state seeded from server value so an empty input is not silently read as "zero it". Documented drift-clobber caveat and the lazy-create story ("stock rows appear on first reservation attempt") so admins do not wonder why the list is empty after a fresh apply |
| 2026-08-04 | Cursor Agent | W6 commit F: Grafana OSS on Fargate (ADR-05). $29/user/mo Managed Grafana replaced by ~$3/mo Grafana OSS on one ARM64 Fargate task. Dedicated Cognito app client (`generate_secret = true` — ALB authenticate-cognito needs a confidential client). Admin password: `random_password` + Secrets Manager, injected via `secrets.valueFrom` (never plaintext env). `GF_SERVER_SERVE_FROM_SUB_PATH=true` so Grafana emits `/grafana`-prefixed assets behind the reverse proxy. ALB listener rule at priority 100 with `authenticate-cognito` action; internal ALB reached via SSM port-forward. Persistence intentionally ephemeral (dashboards live as JSON in `docs/grafana-dashboards/`, to be added; EFS would be the prod path) |
| 2026-08-04 | Cursor Agent | W6 commit G: ADOT collector sidecar on every Fargate task (W5 D4-5 deferral). `public.ecr.aws/aws-observability/aws-otel-collector:v0.42.0` with the built-in `/etc/ecs/ecs-default-config.yaml` preset — fans OTLP receives to X-Ray + CloudWatch EMF. Sidecar `essential=false` so a collector crash cannot kill the app during viva; app `dependsOn` collector `HEALTHY` so the first request never emits into a dead port. Task CPU/memory bumped 256/512 → 512/1024 to fit both containers (Fargate combos are discrete). `OTEL_*` env vars on the app forward-compat for an OTel-SDK migration. Managed policies `AWSXrayWriteOnlyAccess` + `CloudWatchAgentServerPolicy` attached via a for_each map to every task role |
