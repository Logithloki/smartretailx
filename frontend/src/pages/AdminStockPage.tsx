// Real implementation lands in commit E (Admin Stock view + adjust). Stub
// so App.tsx compiles; delete this comment when the page is written.
export function AdminStockPage() {
  return <p>Admin stock page — coming in commit E.</p>;
}
