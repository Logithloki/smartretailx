// Real implementation lands in commit D (Admin Products CRUD). Stub so
// App.tsx compiles; delete this comment when the page is written.
export function AdminProductsPage() {
  return <p>Admin products page — coming in commit D.</p>;
}
