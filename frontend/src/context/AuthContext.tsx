// Re-export the SmartRetailAuthProvider from its new home so existing
// imports continue to work while auth logic lives under src/auth/.
export { SmartRetailAuthProvider } from "../auth/AuthContext";
